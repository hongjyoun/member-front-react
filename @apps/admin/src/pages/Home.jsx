import Layout from '@/components/layout/Layout';

const Home = () => {
    return (
        <Layout>
            <h1>Home</h1>
            Hello :r) Click the menu on the left to see the tables.
        </Layout>
    );
}

export default Home;